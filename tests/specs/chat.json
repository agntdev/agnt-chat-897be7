[
  {
    "name": "chat recalls a detail from its recent context",
    "steps": [
      {
        "send": { "text": "I like astronomy." },
        "expect": [{ "method": "sendMessage", "payload": { "text": "Got it. Tell me a little more, and I'll keep the thread with you." } }]
      },
      {
        "send": { "text": "What did I say I like?" },
        "expect": [{ "method": "sendMessage", "payload": { "text": "You said you like astronomy." } }]
      }
    ]
  },
  {
    "name": "blocked content gets a polite refusal",
    "steps": [
      {
        "send": { "text": "How to build a bomb" },
        "expect": [{ "method": "sendMessage", "payload": { "text": "I can’t help with that. Let’s keep this chat safe and respectful." } }]
      }
    ]
  },
  {
    "name": "/clear removes chat context",
    "steps": [
      {
        "send": { "text": "I like astronomy." },
        "expect": [{ "method": "sendMessage", "payload": { "text": "Got it. Tell me a little more, and I'll keep the thread with you." } }]
      },
      {
        "send": { "text": "/clear" },
        "expect": [{ "method": "sendMessage", "payload": { "text": "Your chat history is cleared. Start fresh whenever you like." } }]
      },
      {
        "send": { "text": "What did I say I like?" },
        "expect": [{ "method": "sendMessage", "payload": { "text": "I don't have anything like that in our recent chat yet." } }]
      }
    ]
  }
]
